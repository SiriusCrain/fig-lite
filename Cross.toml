[build]
pre-build = ["apt-get update && apt-get --assume-yes install unzip zsh"]

[build.env]
passthrough = [
    "BAY_BUILD_TARGET_TRIPLE",
    "BAY_BUILD_VARIANT",
    "BAY_BUILD_HASH",
    "BAY_BUILD_DATETIME",
    "BAY_BUILD_SKIP_FISH_TESTS",
    "BAY_BUILD_SKIP_SHELLCHECK_TESTS",
]
